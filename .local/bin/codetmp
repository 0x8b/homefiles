#!/bin/bash

# https://news.ycombinator.com/item?id=26083919

set -euo pipefail

REPO="$1"
readonly TEMPD="$(mktemp -d)"

if [[ ! $REPO =~ "https://*" ]]; then
  REPO="https://github.com/${REPO}.git"
fi

git clone $REPO $TEMPD

code --wait -n $TEMPD

rm -rf $TEMPD
